//
//  MissionView.swift
//  Life-Manager
//
//  Created by Christiane Roth on 13.04.25.
//
import SwiftUI
/*
struct DummyMissionView: View {
    var body : some View {
        NavigationStack {
            ZStack(alignment: .topTrailing) {
                ScrollView(.horizontal, showsIndicators: false){
                    HStack {
                      
                        Text("Haushalt")
                            .frame(width: 100, height: 30) // Größe des Textfeldes
                            .background(Color.white) // Hintergrundfarbe des Textfeldes
                            .border(Color.black, width: 2) // Rahmen um den Text
                            .cornerRadius(5) // Optionale abgerundete Ecken
                            .padding() // Abstand um den Text
                            .padding(.top, 50) // Abstand vom oberen Bildschirmrand
                        Spacer() // Fügt Platz ein, damit
                        
                        Text("Sport")
                            .frame(width: 100, height: 30) // Größe des Textfeldes
                            .background(Color.white) // Hintergrundfarbe des Textfeldes
                            .border(Color.black, width: 2) // Rahmen um den Text
                            .cornerRadius(5) // Optionale abgerundete Ecken
                            .padding() // Abstand um den Text
                            .padding(.top, 50) // Abstand vom oberen Bildschirmrand
                        Spacer() // Fügt Platz ein, damit
                        
                        Text("Urlaub")
                            .frame(width: 100, height: 30) // Größe des Textfeldes
                            .background(Color.white) // Hintergrundfarbe des Textfeldes
                            .border(Color.black, width: 2) // Rahmen um den Text
                            .cornerRadius(5) // Optionale abgerundete Ecken
                            .padding() // Abstand um den Text
                            .padding(.top, 50) // Abstand vom oberen Bildschirmrand
                        Spacer() // Fügt Platz ein, damit
                        
                        Text("Kinder")
                            .frame(width: 100, height: 30) // Größe des Textfeldes
                            .background(Color.white) // Hintergrundfarbe des Textfeldes
                            .border(Color.black, width: 2) // Rahmen um den Text
                            .cornerRadius(5) // Optionale abgerundete Ecken
                            .padding() // Abstand um den Text
                            .padding(.top, 50) // Abstand vom oberen Bildschirmrand
                        Spacer() // Fügt Platz ein, damit
                    }
                }
                .padding(.top,20)
                
             
                
                
            }
            .padding(.bottom,700)
           
        }
    }
}

#Preview {
    DummyMissionView()
}
*/
